#!/bin/sh

set -eu

list_apt_packages() {
    if [ "${CPU_VENDOR}" = "intel" ]; then
        echo "intel-microcode"
    elif [ "${CPU_VENDOR}" = "amd" ]; then
        echo "amd64-microcode"
    fi
}

"${@}"
