#!/bin/sh

set -eu

if [ "${DEBUG+defined}" != "defined" ]; then
    export DEBUG=1
fi
export DRY_RUN=1
if [ "${ALLOW_NONFREE+defined}" != "defined" ]; then
    ALLOW_NONFREE=1
fi
export ALLOW_NONFREE

`dirname $0`/setup.sh "${@}"
